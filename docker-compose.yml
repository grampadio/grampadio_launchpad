services:
  grampad:
    image: ghcr.io/grampadio/grampad:1.0.0
    container_name: grampad
    ports:
      - "3004:3004"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped